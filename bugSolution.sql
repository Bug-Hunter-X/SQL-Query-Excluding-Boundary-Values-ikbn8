```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
By changing the `>` operator to `>=`, the query now includes employees with salaries of 100000 or greater, ensuring a complete and accurate result set.